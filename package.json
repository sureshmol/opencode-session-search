{
  "name": "@sureshmol/opencode-session-search",
  "version": "0.1.0",
  "description": "Search across all OpenCode sessions with full-text search",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "keywords": [
    "opencode",
    "plugin",
    "search",
    "sessions"
  ],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "bun test",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^25.9.1",
    "bun-types": "^1.3.14",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  }
}
